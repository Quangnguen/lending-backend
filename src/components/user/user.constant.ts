export enum USER_ROLE_ENUM {
  USER,
  OWNER,
  ADMIN,
}

export enum USER_LOCKED_ENUM {
  UNLOCKED,
  LOCKED,
}

export const SALT_ROUNDS_PASSWORD = 8;

export enum USER_GENDER_ENUM {
  MALE,
  FEMALE,
  OTHER,
}
